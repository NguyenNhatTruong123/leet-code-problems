/**
 * @param {number} x
 * @param {number} y
 * @param {number} bound
 * @return {number[]}
 */
var powerfulIntegers = function (x, y, bound) {
    var re = []

    let i = 0, j = 0
    let sum = 2

    while (sum <= bound) {
        re.push(sum)

    }

};